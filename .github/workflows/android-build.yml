name: Build Android APK

on:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Determine Build Mode (Lite vs Full)
      id: mode
      run: |
        COMMIT_MSG="${{ github.event.head_commit.message }}"
        if [[ "$COMMIT_MSG" == *"[full]"* ]]; then
          echo "IS_FULL=true" >> $GITHUB_ENV
          echo "Build mode: FULL"
        else
          echo "IS_FULL=false" >> $GITHUB_ENV
          echo "Build mode: LITE"
        fi

    - name: Download Model Assets for Offline ASR
      run: |
        mkdir -p app/src/main/assets/tilawa_model
        curl -L -o app/src/main/assets/tilawa_model/vocab.json https://github.com/yazinsai/tilawa/releases/download/v0.2.0/vocab.json || true
        if [ "$IS_FULL" = "true" ]; then
          echo "Downloading 88MB ONNX model for FULL build..."
          curl -L -o app/src/main/assets/tilawa_model/model.onnx https://github.com/yazinsai/tilawa/releases/download/v0.2.0/fastconformer_full_mixed.onnx || true
        else
          echo "Skipping 88MB model download for LITE build..."
        fi

    - name: Decode Keystore
      env:
        ENCODED_STRING: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      run: |
        if [ -n "$ENCODED_STRING" ]; then
          echo $ENCODED_STRING | base64 -d > app/keystore.jks
        fi

    - name: Build Android APK
      run: |
        chmod +x gradlew
        ./gradlew assembleRelease

    - name: Sign APK
      env:
        KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
        KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
      run: |
        if [ -f "app/keystore.jks" ]; then
          $ANDROID_HOME/build-tools/34.0.0/apksigner sign --ks app/keystore.jks \
            --ks-key-alias "$KEY_ALIAS" \
            --ks-pass pass:"$KEYSTORE_PASSWORD" \
            --key-pass pass:"$KEY_PASSWORD" \
            app/build/outputs/apk/release/app-release-unsigned.apk
        fi
        if [ "$IS_FULL" = "true" ]; then
          mv app/build/outputs/apk/release/app-release-unsigned.apk app/build/outputs/apk/release/mushaf-qiyam-full.apk
        else
          mv app/build/outputs/apk/release/app-release-unsigned.apk app/build/outputs/apk/release/mushaf-qiyam-lite.apk
        fi

    - name: Create Single Release Asset
      uses: ncipollo/release-action@v1
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        artifacts: ${{ env.IS_FULL == 'true' && 'app/build/outputs/apk/release/mushaf-qiyam-full.apk' || 'app/build/outputs/apk/release/mushaf-qiyam-lite.apk' }}
        tag: "v4.5.0"
        name: "مصحف القيام — V4.5.0 Lite"
        allowUpdates: true
        body: |
          ### مصحف القيام — V4.5.0 Lite (تحديث خفيف)
          تحديث خفيف كود وواجهة وسجل تشخيص فني، يستغل نموذج التلاوة المحفوظ سابقاً على الجهاز.
