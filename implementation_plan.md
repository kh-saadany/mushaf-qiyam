# خطة إعادة التقييم الهيكلي لتطبيق مصحف القيام (التحول إلى Native ASR)

> [!IMPORTANT]
> **ملاحظة هامة:** بناءً على تعليمات المستخدم، تم إيقاف أي تعديل على الأكواد المصدرية للتطبيق بشكل كامل، ويقتصر العمل حالياً على إعادة تقييم الموقف هندسياً، ووضع خطة بديلة لمعالجة بطء الأداء والتعليق على الأجهزة الضعيفة (مثل Samsung Galaxy Tab A7).

---

## 1. أسباب الخلل الهيكلي في التصميم الحالي (الدروس المستفادة)

بعد تحليل سلوك التطبيق في الوضعين (أونلاين وأوفلاين) على الأجهزة المختلفة، تبين أن المشكلة ليست في جودة كتابة الكود بل في **فلسفة التصميم الأساسية (Architectural Design)**:

### أولاً: وضع الأوفلاين الحالي (Transformers.js + ONNX Runtime Web)
* **المشكلة:** يعتمد التصميم الحالي على تشغيل نموذج Whisper-tiny داخل الـ Web Worker الخاص بمتصفح WebView (من خلال مكتبة WebAssembly).
* **سبب الفشل على الأجهزة الضعيفة:**
  1. **معالجة أحادية الخيط (Single-Threaded WASM):** تطبيق Capacitor يعمل محلياً ويفتقر إلى ترويسات الأمان الخاصة بالمتصفحات العادية (Cross-Origin Isolation Headers like COOP/COEP). بدون هذه الترويسات، تحظر الهواتف استخدام الـ `SharedArrayBuffer` وهو المكون الأساسي لتوزيع المعالجة على أنوية المعالج المتعددة. هذا يجبر نموذج Whisper الضخم (حتى بنسخته الـ q4 المكممة) على العمل على **نواة واحدة فقط** وبدون تسريع عتادي (No Hardware Acceleration).
  2. **بطء زمن الاستدلال (Inference Latency):** معالجة مقطع صوتي مدته 4 ثوانٍ على معالج ضعيف مثل Snapdragon 662 (الموجود في تابلت A7) تستغرق ما بين **8 إلى 15 ثانية**. هذا يؤدي لتراكم البيانات الصوتية في الذاكرة المؤقتة (Queue) تدريجياً حتى ينهار التطبيق تماماً بسبب نفاذ الذاكرة (OOM).

### ثانياً: وضع الأونلاين الحالي (Android SpeechRecognizer API)
* **المشكلة:** يلاحظ المستخدم تأخراً كبيراً يصل إلى 10-15 ثانية قبل تقليب الصفحات.
* **سبب الفشل:**
  * تم تصميم خدمات التعرف على الصوت السحابية (مثل مساعد جوجل أو لوحة المفاتيح) لتسجيل الصوت والانتظار حتى يقوم المتحدث بـ **وقفة صمت واضحة (Speech Pauses/Segmentation)** لمعالجة الجملة وإرجاعها كاملة.
  * في الصلاة، يقرأ الإمام بتدفق مستمر وتجويد متصل، مما يجعل محرك التعرف ينتظر لمدد طويلة قبل أن يكتشف "نهاية الجملة" ويرسل النص للـ WebView. هذا التراكم يجعل المتابعة متأخرة جداً وغير صالحة للاستخدام العملي.

---

## 2. تحليل التجارب السابقة ومحركات الصوت المناسبة لأجهزة التابلت الضعيفة

عند دراسة التطبيقات التفاعلية الشهيرة مثل **تسميع (Tasmee)** (والذي تم إطلاقه عام 2014 ويعمل بكفاءة على أجهزة ضعيفة للغاية بذاكرة 512 ميجابايت) وتطبيق **ترتيل (Tarteel)** في بداياته، نجد المبادئ التالية:

### الدرس الأول: فصل معالجة الصوت تماماً عن الـ WebView
يجب ألا يلمس الـ JavaScript أو الـ WebAssembly أي دفقات صوتية خام أو عمليات ذكاء اصطناعي. يجب أن تتم معالجة الصوت بالكامل بلغة برمجية قريبة من العتاد (C++ أو Java/Kotlin الناتجة عن الكود المترجم للنظام) لضمان الوصول المباشر لمعالج الإشارات الرقمية (DSP) وتفعيل تسريع العتاد (NEON SIMD instructions).

### الدرس الثاني: استخدام محرك Vosk-Android-SDK بدلاً من Whisper
* **حجم النموذج:** نموذج Vosk العربي الصغير (`vosk-model-small-ar-0.3`) يبلغ حجمه **38 ميجابايت فقط** (مقارنة بـ Whisper-tiny الذي يتخطى 75-100 ميجابايت مع توابع المكتبات).
* **سرعة فائقة:** Vosk مبني على Kaldi (HMM-DNN) وهو أسرع بمراحل ضخمة من معالجات الـ Transformers المعقدة. يقوم بمعالجة الصوت في أجزاء من الثانية (أقل من 20 ملي ثانية لكل دفقة صوتية) ولا يستهلك سوى **50-80 ميجابايت** من ذاكرة الرام الكلية للجهاز.
* **العمل أوفلاين 100%:** يعمل محلياً بالكامل ومبني كـ Native Android Library (.so) مبرمجة بلغة C++.

### الدرس الثالث: تقييد القاموس اللغوي (Grammar-Restricted ASR)
* أحد أقوى ميزات Vosk هو إمكانية تزويده بـ **قائمة مفردات محددة (Grammar/Vocabulary)** بدلاً من تركه يبحث في قاموس اللغة العربية بالكامل.
* بما أن النص المستهدف هو القرآن الكريم حصراً (وهو نص ثابت ومحدود الكلمات)، يمكننا حصر الكلمات التي يبحث عنها المحرك في كلمات الجزء أو السورة المقروءة فقط.
* **النتيجة:** ترتفع دقة التعرف الصوتي إلى ما يقرب من **100%**، وينخفض استهلاك المعالج إلى **شبه الصفر** لأن محرك البحث اللغوي لا يبحث في ملايين الاحتمالات بل في بضع مئات من الكلمات القرآنية المحددة مسبقاً للركعة الحالية.

---

## 3. الخطة البديلة المقترحة (تصميم Native Hybrid ASR)

تعتمد الخطة البديلة على إعادة توزيع الأدوار برمجياً داخل التطبيق كما يلي:

```mermaid
graph TD
    subgraph Native Android Layer (Java/Kotlin)
        A[Native AudioRecord] -->|16kHz Mono PCM| B[Vosk Android Recognizer]
        C[(Local Model assets/files)] -->|Initialize| B
        B -->|Real-time Word Events| D[Capacitor JS Bridge]
    end
    
    subgraph Web View Layer (HTML/JS)
        D -->|Receive partial text| E[JavaScript Controller]
        F[(Quran Local Database)] -->|Match Words| E
        E -->|Update UI / Turn Page| G[HTML Mushaf View]
    end
```

### خطوات التنفيذ المقترحة (عند الموافقة على البدء):

1. **الخطوة الأولى: تنظيف الـ WebView**
   * إزالة مكتبة `transformers.js` وخيوط المعالجة الخلفية (`whisper-worker-v2.js`) وملفات الـ WASM بالكامل من مجلد `www/`.
   * تقليص استهلاك ذاكرة الـ WebView ومساحة التخزين الخاصة بها (لتنخفض مساحة التطبيق الكلية ويرتاح المعالج).

2. **الخطوة الثانية: دمج مكتبة Vosk في الأندرويد**
   * إضافة مكتبة Vosk إلى ملف `android/app/build.gradle`:
     ```groovy
     implementation 'org.alphacephei:vosk-android:0.3.45'
     ```
   * تنزيل النموذج الصوتي العربي الصغير وتضمينه داخل مجلد الـ `assets` الخاص بأندرويد، أو تهيئة آلية لتحميله مرة واحدة وحفظه في التخزين الداخلي للتطبيق (`filesDir`).

3. **الخطوة الثالث: بناء Capacitor Plugin مخصص للصوت**
   * تعديل أو إعادة كتابة الـ Plugin الداخلي ليتولى المهام التالية:
     * فتح الميكروفون برمجياً باستخدام `AudioRecord` من نظام أندرويد مباشرة.
     * تهيئة Vosk Recognizer وتزويده بالنموذج المحلي.
     * توفير دالة للويب لتحديث "القاموس المستهدف" (Grammar List) بناءً على السورة التي اختارها المستخدم للركعة.
     * إرسال الكلمات التي ينطقها الإمام أولاً بأول وبشكل لحظي (Partial Results) إلى واجهة الويب.

4. **الخطوة الرابعة: استقبال النص ومطابقته في الويب**
   * يستمع كود الجافا سكريبت في الـ WebView للنصوص القادمة من الـ Plugin.
   * يقوم بعملية مطابقة الكلمات (Word Matching) وتحديث الصفحة بشكل لحظي وسريع جداً (زمن استجابة متوقع أقل من 200 ملي ثانية).

---

## 4. أسئلة مفتوحة للمناقشة وتحديد الأولويات

قبل البدء في أي عمل، نرجو مراجعة التساؤلات التالية لضبط مسار الخطة:

1. **النموذج الصوتي وتضمينه:** هل تفضل دمج الموديل الصوتي العربي الصغير لـ Vosk (~38MB) مباشرة داخل التطبيق ليكون جاهزاً فور التثبيت بدون إنترنت (مما سيرفع حجم الـ APK إلى حوالي 65MB)، أم تفضل إبقاء حجم الـ APK صغيراً مع تحميل الموديل عند التشغيل الأول فقط؟
2. **تابلت الثانوية العامة:** هل التابلت المستهدف مغلق بـ Knox (منظومة الوزارة) أم مفتوح بالكامل ويسمح بتثبيت أي APK بحرية؟ (لضمان عمل الصلاحيات ومكتبات الصوت دون قيود من نظام الحماية المدرسي).
3. **تحديد نطاق القراءة:** لرفع كفاءة وموثوقية البحث الصوتي لأقصى حد، هل تؤيد فكرة إلزام المستخدم باختيار السورة (أو السور) التي سيقرأها الإمام في الصلاة مسبقاً من شاشة الإعدادات لتمريرها كـ Grammar لمستمع Vosk؟
